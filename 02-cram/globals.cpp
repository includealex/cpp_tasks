// globals.cpp
#include "globals.h"

std::vector<int> memory(100); // Define and initialize memory
int c = 0;                    // Define synonym for '['
int x = 0;                    // Define synonym for ']['
